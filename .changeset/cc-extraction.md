---
'@cogito.ai/cc': minor
---

First release of `@cogito.ai/cc` (`cc`) — the game-specific CLI line extracted from AgentDock: template registry + `init` + skills channel + auth. Ships the game-web-phaser template.
