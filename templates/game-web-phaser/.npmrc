# 只有这个模板的 .npmrc 写 registry=，其余两个模板（web-nextjs / skills-registry）
# 只有注释、没有 registry 行——这不是疏忽，是范围选择：它们没被下面这个问题挡住。
#
# 为什么必须指定 registry：
# - 本机默认源 registry.npmmirror.com 上的 phaser 副本停在 2026-04-10、最高只到 4.0.0，
#   `phaser@4.2.1` 在那个源上直接 404（实测，不是推断）。
# - 主因是包体大：4.2.1 解包 112 MB / 3593 文件，压缩后 19.5 MB，npmmirror 没有同步到这个版本。
# - 三个实测过的源里，腾讯源最快也确实有货：mirrors.tencent.com/npm 装 19.5 MB @ 7.1 MB/s（2.75s）；
#   华为源 4.3 MB/s；官方源 3.3 MB/s。
#
# 写在模板级而不是仓库级：见 openspec/changes/phaser4-template-upgrade/design.md D2——
# 仓库根 .npmrc 的 registry= 优先级高于用户级配置，会连带覆盖 CI 发布用的官方 registry，
# 把 changeset publish 指向一个只读镜像、弄坏发布链路。模板级只影响脚手架出来的生成项目
# （构建期 CLI 会把这个文件改名成 _npmrc 随包发布，脚手架时再还原成 .npmrc，见
# packages/cli/src/core/scaffold.ts 的 restoreDotfiles 与 packages/cli 的 build 脚本），
# 顺带也消掉了"VM 里装包用哪个源"这个原本查不到的未知数。
registry=https://mirrors.tencent.com/npm/
